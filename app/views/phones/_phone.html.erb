				<tr>
					<th><%= phone.id %></th>
					<th><%= phone.status %></th>
					<th><%= phone.nphone %></th>
					<th><%= link_to "<button>Delete Phone</button>".html_safe, [phone.contact, phone], method: :delete, data: { confirm: 'Are you sure?' }, :class => 'button', :id => 'button7' %></th>
				</tr>
